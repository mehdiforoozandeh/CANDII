"""The naive baseline suite (`RIVALS_PLAN.md` §5).

`heads.py` is the arithmetic, `generate.py` writes the §4.1 prediction roots, `leaderboard.py`
folds the score files `candi.bench.external` writes into one table. Nothing here scores anything.
"""
